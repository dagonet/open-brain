# Claude Code - machine-specific files (not committed)
# .claude/settings.json and .claude/agents/ ARE committed (shared with team)
CLAUDE.local.md
.claude/.mcp.json
.mcp.json

# Environment
.env
.env.local

# Dependencies
node_modules/

# Supabase local
supabase/.temp/

# Build outputs
dist/
build/
.next/
next-env.d.ts
*.tsbuildinfo

# Git worktrees
.worktrees/
.claude/worktrees/

# Local notes / scratch (not committed)
nate_new_video_open-brain_update.txt

# Gate artifact — machine-local, 60-minute expiry, keyed to local HEAD
/.gate/

# Playwright MCP scratch output — machine-local, regenerated on demand
/.playwright-mcp/
